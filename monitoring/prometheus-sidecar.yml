global:
  scrape_interval: 15s
  evaluation_interval: 15s

# For now, we'll just scrape and store locally
# The sidecar makes metrics available at http://localhost:9091
# TODO: Configure OTLP bridge to send to SigNoz
# remote_write:
#   - url: http://k12-signoz-otel-collector:4318/v1/metrics
#     send_exemplars: true
#     write_relabel_configs:
#       - source_labels: [__name__]
#         regex: 'go_|process_|java_lang_'
#         action: drop

scrape_configs:
  - job_name: 'k12-backend'
    static_configs:
      - targets: ['k12-backend:8080']
        labels:
          service_name: k12-backend
          deployment_environment: development
    metrics_path: /metrics
    scrape_interval: 15s
